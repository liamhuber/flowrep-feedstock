schema_version: 1

context:
  name: flowrep
  version: "0.6.0"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/flowrep-${{ version }}.tar.gz
  sha256: 4132b58c15b45fc6b3afb2beb3e1365c488db4d8bfdef59fc93af09228e88dbc

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=${{ python_min }},<3.15
    - packaging >=24.2,<=26.2
    - pydantic >=2.12.0,<2.13.0
    - pyiron_snippets >=1.3.0,<2.0.0

tests:
  - python:
      imports:
        - flowrep
      pip_check: true
      python_version: ${{ python_min }}

about:
  summary: flowrep - Your premier tool for workflow representations
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/pyiron/flowrep/

extra:
  recipe-maintainers:
    - samwaseda
    - jan-janssen
    - liamhuber
