schema_version: 1

context:
  name: flowrep
  version: "0.5.2"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/flowrep-${{ version }}.tar.gz
  sha256: 6a674d18ec6cf6ef6307caaba092aedd62b3bcc7641f41a71be3fcb28b7ffb90

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >=${{ python_min }},<3.15
    - pydantic >=2.12.0,<2.13.0
    - pyiron_snippets >=1.3.0,<2.0.0

tests:
  - python:
      imports:
        - flowrep
      pip_check: true
      python_version: ${{ python_min }}

about:
  summary: flowrep - Your premier tool for workflow representations
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/pyiron/flowrep/

extra:
  recipe-maintainers:
    - samwaseda
    - jan-janssen
    - liamhuber
